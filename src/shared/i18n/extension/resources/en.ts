import { ResourceTranslations } from "./type";
import { en as ogameResources } from '../../ogame/resources/en';

export const en: ResourceTranslations = {
    ...ogameResources,
    sum: 'Sum of resources',
    sumMsu: 'Sum of resources (MSU)',
};