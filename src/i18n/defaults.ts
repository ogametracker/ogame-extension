const defaultLocale: Locale = {
    code: 'de-DE',
    options: {
        weekStartsOn: 1
    },
};

const defaultDateFormat = 'dd.MM.yyyy';

export {
    defaultLocale, 
    defaultDateFormat,
};